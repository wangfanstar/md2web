# Authenticated editing service dependencies (single set for Python 3.6.8+)
#   python3 -m pip install -r server/requirements.txt
# Verified on Python 3.6.8 / 3.8.5 / 3.12: login, drafts, SVN commit all work.
# Build and read-only preview need no third-party packages (stdlib only).
Flask==2.0.3
Werkzeug==2.0.3
Jinja2==3.0.3
MarkupSafe==2.0.1
itsdangerous==2.0.1
click==8.0.4
blinker==1.4
waitress==2.0.0
# Backports needed on Python 3.6 only (skipped automatically on 3.7+)
dataclasses==0.8; python_version < "3.7"
importlib-metadata==4.8.3; python_version < "3.8"
zipp==3.6.0; python_version < "3.8"
typing-extensions==4.1.1; python_version < "3.8"
