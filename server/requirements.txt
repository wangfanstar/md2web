# Authenticated editing service dependencies (Python 3.8+)
#   python -m pip install -r server/requirements.txt
# Offline intranet: python -m pip download -r server/requirements.txt -d wheels
#                   python -m pip install --no-index --find-links wheels -r server/requirements.txt
# Python 3.6.8 servers: use server/requirements-py36.txt instead.
flask==3.0.3
waitress==3.0.0
