# SVN 配置与冲突修复

目标：用户只需配置 SVN 地址与更新频率，仓库 ID 自动分配并保留既有绑定；维护操作折叠展示。

- [ ] 先补 Node 行为测试，覆盖新目录读取、自动 ID 重名、移除映射及频率校验。
- [ ] 修正 `web/md2web-config.js` 的行状态读取和自动 ID 分配；已保存 ID 不变，新增 ID 避开已有入口文件名与网站备份保留 ID。
- [ ] `web/md2web_config.html` 将地址与频率置于第一行，ID 作为辅助信息，维护操作折叠。
- [ ] 补 Python 回归测试，修复 `server/operations.py` 在远端版本不变时丢失同步冲突的问题，拒绝非有限更新频率。
- [ ] `web/plot-playground.html` 增加返回全部文档链接；更新 README.md、AGENTS.md。
- [ ] 使用 Python 3.6.8 全量测试、离线构建；运行 Node 测试与语法检查，核对源文档不变，提交并推送 GitHub。

验证重点：新旧仓库 ID 稳定、移除映射后不自动恢复、非法频率保存前报错、草稿冲突持续可见且解除后同步补齐。现有 SVN 凭据、提交状态机及绑定校验保持原有流程。
